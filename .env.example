# Optional scheduler mode. Default is fixture; set oanda only to opt in explicitly.
NAS100_DASHBOARD_SCHEDULER_PROVIDER=fixture

# OANDA credentials are local-only and must never be committed.
OANDA_ENVIRONMENT=practice
OANDA_ACCOUNT_ID=
OANDA_API_TOKEN=
OANDA_NAS100_INSTRUMENT=
